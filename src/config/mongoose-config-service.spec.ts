import { MongooseConfigService } from './mongoose-config-service';

describe('MongooseConfigService', () => {
  it('should be defined', () => {
    expect(new MongooseConfigService()).toBeDefined();
  });
});
